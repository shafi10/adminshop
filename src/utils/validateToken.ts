// import { getToken } from "./get-token";
// import { config } from "../config";
// import jwt from "jsonwebtoken";

export const validateToken = (tokenStr: string) => {
  // const token: string = tokenStr || getToken() || "";
  // const info = jwt.verify(
  //   token,
  //   config?.ADMINJWTSECRET,
  //   (err: any, decode: any) => {
  //     if (err) {
  //       return false;
  //     }
  //     return decode;
  //   }
  // );
  // console.log(info);
};
